logging:
  level:
    com.zaxxer:
      hikari.HikariConfig: debug
    org.axonframework: debug
    org.springframework.security: debug
    org.jgroups: debug
    org.dyson.dddstarter: debug
spring:
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true
